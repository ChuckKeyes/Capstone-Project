# Lab 1 — Branching, Git, & Python

## Overview

I worked on my branch of the group's repo, and added my files, committed my changes, pushed them to github, and used a pull request to merge my work.


For my project, I made a simple math quiz. It asks the user a question, checks the answer, and can create a score file, uses input, variables, conditionals, file creation, and multiple python files.

## How to Run

Run `app.py` in VScode


## Files

- `app.py` - runs the quiz
- `quiz_game.py` - has the quiz code
- `README.md` - explains what I did

## Git Practice

Fixing common Git issues.

I staged files: 
- `notes.txt` 
- `debug.log`
- `test_output.tmp`

then removed them from stage before committing.

I ran `git commit` without `-m`, corrected the non-commented commit, and finished it.

For the merge conflict simulation, I changed a line in `app.py` in different branches, merged them, then fixed the conflict in `app.py`, and committed the fixed file.

Screenshot for the Git troubleshooting:

* `1.2.1_Visuals`
* `1.2.2_Visuals`
* `1.2.3_Visuals`
